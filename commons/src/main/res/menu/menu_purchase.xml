<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <item
        android:id="@+id/changeStore"
        android:title="@string/billing_change_to_google_play"
        android:visible="false"
        app:showAsAction="always"
        tools:visible="true" />
    <item
        android:id="@+id/restorePurchases"
        android:icon="@drawable/ic_restore_purchase"
        android:title="@string/restore_purchase"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/openSubscriptions"
        android:title="@string/billing_subscriptions"
        app:showAsAction="never"/>
</menu>
