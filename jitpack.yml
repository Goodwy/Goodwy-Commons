jdk:
    - openjdk17
before_install:
    - apt-get update
    - apt-get install -y cmake
build:
    commands:
    - ./gradlew assembleСoreRelease
    - ./gradlew assembleFossRelease
    - ./gradlew assembleRustoreRelease
