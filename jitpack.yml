jdk:
    - openjdk17
before_install:
    - sudo apt-get update
    - sudo apt-get install -y cmake
install:
    - ./gradlew :commons:publishToMavenLocal
    - ./gradlew :commons:clean :commons:publishToMavenLocal
